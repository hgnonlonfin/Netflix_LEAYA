import webbrowser

id = #imdbId, comment sont identifiés les suggestions en sortie du 
res = 7 - len(str(id))
x = '0'*res + str(id)

url = 'https://www.imdb.com/title/tt' + x + '/?ref_=fn_al_tt_1'

webbrowser.open(url, new=1, autoraise=True)

#Attention depuis l'IDE Pycharm ça ouvre un nouvel onglet 
#dans un navigateur déjà ouvert, ne semble pas fonctionné dans Google Collabs!
